# Runtime dependencies for the Vercel deployment of the Encounter Object ID
# web interface (api/index.py). Kept lean to fit the serverless bundle:
# no torch/transformers (uses the perceptual embedder) and no lxml
# (BeautifulSoup falls back to the stdlib html.parser).
fastapi>=0.110
pydantic>=2.6
pydantic-settings>=2.1
python-multipart>=0.0.9
SQLAlchemy>=2.0
psycopg[binary]>=3.1
httpx>=0.27
beautifulsoup4>=4.12
numpy>=1.26
pillow>=10.2
